package com.rsn.carousel;

import org.junit.Assert;
import org.junit.Test;

import java.util.Arrays;
import java.util.Collections;

public class CarouselDemoTest {

//    @Test
//    public void rotateLeftTest() {
//        CarouselDemo demo = new CarouselDemo();
//
//        Assert.assertEquals(Arrays.asList(2, 3, 4, 1), demo.rotateRight(Arrays.asList(1, 2, 3, 4)));
//        Assert.assertEquals(Arrays.asList(4, 3), demo.rotateRight(Arrays.asList(3, 4)));
//        Assert.assertEquals(Arrays.asList(4), demo.rotateRight(Arrays.asList(4)));
//
//        Assert.assertEquals(Collections.emptyList(), demo.rotateRight(null));
//        Assert.assertEquals(Collections.emptyList(), demo.rotateRight(Collections.emptyList()));
//    }

}